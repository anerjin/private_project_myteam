/**
 * 도우미의 이름.
 *
 * **Neowave** 의 앞을 땄습니다. 이름을 물으면 그렇게 답합니다.
 *
 * 처음 이름은 「디오」로 `DOI(드론 공간정보)` 를 소리 나는 대로 읽은
 * 것이었습니다. 이 시스템이 DOI 개발팀의 것이 아니라 네오웨이브 운영
 * 시스템이라 정정되면서 이름도 그 자리를 따라 옮겼습니다.
 *
 * ## 왜 별도 파일인가
 *
 * 이 이름을 **화면과 서버가 둘 다** 씁니다 — 패널의 문구·`aria-label` 과
 * 프롬프트의 「당신의 이름은」입니다. 그런데 `chat.service` 는 `server-only`
 * 라 화면이 못 읽고, 패널은 `"use client"` 라 서버가 못 읽습니다.
 *
 * 양쪽에 하나씩 두면 **한 사실이 두 곳**에 생기고, 이름을 바꾸는 날 한 곳만
 * 바뀌어 「어디는 네오, 어디는 도우미」가 됩니다. `page-context` 를 같은
 * 이유로 이 폴더에 둔 것과 같은 자리입니다.
 */
export const ASSISTANT = "네오";
